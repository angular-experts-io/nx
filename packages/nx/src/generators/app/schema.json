{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "app",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Application name"
    },
    "context": {
      "type": "string",
      "description": "Application context (eg b2e, aro, mcs, mcm,...)"
    },
    "prefix": {
      "type": "string",
      "description": "Prefix (for example your company abbreviation)"
    }
  },
  "required": []
}
