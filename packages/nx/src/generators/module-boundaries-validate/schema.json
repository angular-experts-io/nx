{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "module-boundaries-validate",
  "type": "object",
  "properties": {
    "fix": {
      "type": "boolean",
      "description": "Auto fix project.json tags violation, will not fix folder structure and .eslintrc.json violations, those must be fixed manually...",
      "default": false
    }
  },
  "required": []
}
